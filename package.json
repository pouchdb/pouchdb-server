{
	"name": "pouchdb-show",
	"main": "index.js",
	"scripts": {
		"build-js": "mkdir -p ../../dist && browserify index.js -s Show -o ../../dist/pouchdb-show.js",
		"build": "npm run build-js; cd ../../dist; uglifyjs pouchdb-show.js -mc > pouchdb-show.min.js"
	}
}
